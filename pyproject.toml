[build-system]
requires = [
    "setuptools>=42, <71",
    "wheel",
    "pybind11~=2.13.6",
    "scikit-build>=0.7.0",
    "cmake>=3.5.3",
    "ninja",
    "packaging<22"
]
build-backend = "setuptools.build_meta"
